[phases.setup]
  nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.build]
  cmds = [
    "cd client && npm ci && npm run build",
    "cd server && npm ci"
  ]

[start]
  cmd = "node server/src/index.js"
