{
  "name": "tgstore",
  "version": "1.0.0",
  "private": true,
  "description": "Unlimited cloud drive powered by Telegram",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",

    "icons": "node client/generate-icons.mjs",

    "android:init": "npm run build && npx --prefix client cap add android",
    "android:sync": "npm run build && npx --prefix client cap sync android",
    "android:open": "npx --prefix client cap open android",
    "android:run":  "npx --prefix client cap run android",

    "android:build": "npm run build && npx --prefix client cap sync android && npx --prefix client cap build android"
  }
}
