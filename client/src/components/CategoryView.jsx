export default function CategoryView({ loading, category, folders, navigate, onNewFolder, categoryId }) {
  return (
    <div className="flex-1 overflow-auto">
      <div className="sticky top-0 z-10 bg-white border-b border-slate-200 px-6 py-4"><div className="flex items-center justify-between"><div><h2 className="text-lg font-semibold text-slate-800">📁 {category?.name || 'Category'}</h2><p className="text-sm text-slate-500">Channel: {category?.channel_id ? 'Active' : 'Creating...'}</p></div><button onClick={onNewFolder} className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700">+ New Folder</button></div></div>
      <div className="p-6">{loading ? <div className="flex items-center justify-center py-24"><div className="h-8 w-8 animate-spin rounded-full border-4 border-indigo-200 border-t-indigo-600" /></div> : folders.length === 0 ? (<div className="flex flex-col items-center justify-center py-24 text-center"><div className="text-6xl mb-4">📂</div><h3 className="text-lg font-medium text-slate-700">No folders yet</h3><p className="text-sm text-slate-500 mt-1">Create a folder to start organizing</p><button onClick={onNewFolder} className="mt-4 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white hover:bg-indigo-700">Create First Folder</button></div>) : (<div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-4">{folders.map(folder => (<button key={folder.id} onClick={() => navigate(`/app/category/${categoryId}/folder/${folder.id}`)} className="flex flex-col items-center p-4 rounded-xl border border-slate-200 hover:border-indigo-300 hover:bg-indigo-50 transition text-center"><div className="text-4xl mb-2">📁</div><p className="text-sm font-medium text-slate-700 truncate w-full">{folder.name}</p><p className="text-xs text-slate-400 mt-1">{folder.itemCount || 0} items</p></button>))}</div>)}</div>
    </div>
  );
}