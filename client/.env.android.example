# Copy this to client/.env.production before running `npm run android:build`
# Replace the URL with your publicly accessible server (HTTPS recommended).

# Required: full base URL of your TGStore server (no trailing slash)
VITE_API_BASE_URL=https://your-server.com
